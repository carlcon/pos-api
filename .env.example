# =============================================================================
# POS API - Environment Variables Example
# =============================================================================
# Copy this file to .env and fill in the values
# NEVER commit .env to version control!
#
# For production deployment, these values are set via GitHub Secrets
# and automatically injected during CI/CD deployment.
# =============================================================================

# -----------------------------------------------------------------------------
# Django Settings
# -----------------------------------------------------------------------------
# Generate a new secret key with:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=your-super-secret-key-here

# Set to False in production
DEBUG=True

# Comma-separated list of allowed hosts
# Example: your-domain.com,www.your-domain.com,your-ec2-ip
ALLOWED_HOSTS=localhost,127.0.0.1

# -----------------------------------------------------------------------------
# Database Settings (PostgreSQL)
# -----------------------------------------------------------------------------
DB_NAME=pos_db
DB_USER=pos_user
DB_PASSWORD=your-strong-database-password
DB_HOST=localhost
DB_PORT=5432

# -----------------------------------------------------------------------------
# CORS & CSRF Settings
# -----------------------------------------------------------------------------
# Frontend URL(s) - comma-separated for multiple origins
# Example: https://your-domain.com,https://www.your-domain.com
CORS_ALLOWED_ORIGINS=http://localhost:3000
CSRF_TRUSTED_ORIGINS=http://localhost:3000

# -----------------------------------------------------------------------------
# Frontend Settings
# -----------------------------------------------------------------------------
# Full URL to the API (used by Next.js frontend)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# -----------------------------------------------------------------------------
# OAuth2 Settings
# -----------------------------------------------------------------------------
OAUTH2_PROVIDER_ACCESS_TOKEN_EXPIRE_SECONDS=36000
OAUTH2_PROVIDER_REFRESH_TOKEN_EXPIRE_SECONDS=86400

# -----------------------------------------------------------------------------
# Docker/ECR Settings (Set automatically by CI/CD)
# -----------------------------------------------------------------------------
# ECR_REGISTRY=your-aws-account-id.dkr.ecr.region.amazonaws.com
# IMAGE_TAG=latest

# =============================================================================
# GitHub Secrets Required for CI/CD
# =============================================================================
# Add these secrets in: GitHub Repo → Settings → Secrets and variables → Actions
#
# AWS Credentials:
#   AWS_ACCESS_KEY_ID      - IAM user access key
#   AWS_SECRET_ACCESS_KEY  - IAM user secret key
#   AWS_REGION             - e.g., us-east-1
#
# EC2 Access:
#   EC2_HOST               - EC2 public IP or domain
#   EC2_SSH_KEY            - Base64-encoded private SSH key
#                            Generate with: cat your-key.pem | base64
#
# Application Secrets:
#   SECRET_KEY             - Django secret key
#   DB_PASSWORD            - PostgreSQL password
#   DB_NAME                - Database name (optional, defaults to pos_db)
#   DB_USER                - Database user (optional, defaults to pos_user)
#   ALLOWED_HOSTS          - Comma-separated allowed hosts
#   CORS_ALLOWED_ORIGINS   - Frontend URL(s)
#   CSRF_TRUSTED_ORIGINS   - Frontend URL(s)
#   NEXT_PUBLIC_API_URL    - API URL for frontend
# =============================================================================
